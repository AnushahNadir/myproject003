import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';
import './Hello.js';


ReactDOM.render( <App></App>,
  document.querySelector('#root')
  );










/*import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';
*/

/*ReactDOM.render(
 <div> <App name="Anu" age="21"/> </div>,
document.getElementById('root')
);

reportWebVitals();*/


/*ReactDOM.render(
    //<App/>, we can do this as well
    //here age="21" this is string but if we want to accept number here then we'll have to use the syntax provided by JS
    //which is age={21}
  <div> <App name="Anu" age="21"/> </div>,//here we initialized a name property
  document.getElementById('root')
);*/

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals

